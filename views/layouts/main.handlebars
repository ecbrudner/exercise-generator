<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gym Genius</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Add custom dark green theme */
    :root {
      --primary-green: #064e3b; /* Deep dark green */
      --secondary-green: #10b981; /* Lighter accent green */
    }
  </style>
</head>

<body class="bg-gray-100 font-sans">
  <div class="min-h-screen flex flex-col">
    <!-- Navbar -->
    <header class="bg-[var(--primary-green)] text-white p-4 flex justify-between items-center shadow-md">
      <h1 class="text-2xl font-bold tracking-wide">Gym Genius</h1>
      <nav class="space-x-4">
        {{#if loggedIn}}
          <a href="/profile" class="text-lg hover:text-gray-300">Profile</a>
          <button class="bg-red-600 px-4 py-2 rounded-md text-white hover:bg-red-500" id="logout">Logout</button>
        {{else}}
          <a href="/login" class="text-lg hover:text-gray-300">Login</a>
        {{/if}}
      </nav>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto mt-10 p-5">
      {{{ body }}}
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white text-center py-4 mt-auto">
      <p>© 2025 Gym Genius. All rights reserved.</p>
    </footer>
  </div>

  {{#if loggedIn}}
    <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>
